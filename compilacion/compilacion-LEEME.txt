PROYECTO EDA2 - CARPETA: COMPILACIÓN
------------------------------------
¿Que contiene esta carpeta?
    -> Es una carpeta de trabajo temporal para el compilador. Sirve para almacenar los archivos objeto (.o)
    y acelerar el proceso de construcción del programa.

CONTENIDO:
- Archivos temporales generados automáticamente por VSC. 
- NOTA: Esta carpeta está excluida de Git con .gitignore para evitar subir archivos pesados e innecesarios al repo.

    #Ni se gasten en mirar qué hay adentro. De hecho, puse esta carpeta en el .gitignore para que no se suba nada
    de esto al repo, así mantenemos el GitHub limpito y solo con lo importante.